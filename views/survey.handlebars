<div class="text-center">
  <h2 id="surveyName">{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>

  {{#each questions as |question| }}
    <p>{{question.question}}</p>
  {{#if question.answer1}}
    <button class="buttsClass">{{question.answer1}}</button>
  {{/if}}
  {{#if question.answer2}}
  <button class="buttsClass">{{question.answer2}}</button>
  {{/if}}
  {{#if question.answer3}}
  <button class="buttsClass">{{question.answer3}}</button>
  {{/if}}
  {{#if question.answer4}}
  <button class="buttsClass">{{question.answer4}}</button>
  {{/if}}
  <div style="margin-bottom: 15px"></div>
  {{/each}}

</div>

<script src="./js/form.js"></script>